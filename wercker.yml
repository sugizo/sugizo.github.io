box: ubuntu
build:
  steps:
    - script:
        name: install composer
        code: |
          apt update
          apt install -y composer unzip
    - script:
        name: install jigsaw
        code: composer show | grep jigsaw || composer require tightenco/jigsaw
    - script:
        name: build
        code: ./vendor/bin/jigsaw build
