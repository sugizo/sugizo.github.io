box: node
build:
  steps:
    #- npm-install
    - script:
        name: install
        code: npm list harp || npm install harp
    - script:
        name: compile
        code: ./node_modules/.bin/harp compile
