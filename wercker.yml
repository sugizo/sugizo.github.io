box: ubuntu
build:
  steps:
    - script:
        name: install
        code: |
          apt update
          apt install -y npm nodejs-legacy
          npm list harp || npm install harp -g
    - script:
        name: compile
        code: harp compile
